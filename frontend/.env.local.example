# Leave unset to use same-origin `/api` with Next.js rewrites (see next.config.mjs).
# Set for direct cross-origin API (e.g. Vercel → Render); configure backend CORS + cookies.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
